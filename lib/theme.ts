import { createTheme } from "@mantine/core";

export const theme = createTheme({
    defaultGradient: { from: "violet", to: "orange" },
    black: "#000",
    primaryColor: "dark",
    defaultRadius: "md",
});
